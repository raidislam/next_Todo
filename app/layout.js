import "../styles/app.scss";
import Header from "./header";

export const metadata = {
  title: "Next.js",
  description: "Generated by Next.js App",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <Header />
        {children}
      </body>
    </html>
  );
}
